import 'package:flutter/material.dart';

class NoEncountersFound extends StatelessWidget {
  const NoEncountersFound({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('No encounters found :(');
  }
}
